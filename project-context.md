# プロジェクト概要

個人開発として、子ども（主に小学生程度）を対象とした「学習 × RPG」形式のスマートフォンアプリを開発する。

本プロジェクトの主目的は収益化やサービスとしての差別化ではなく、Web開発を中心としてきた開発者が、React Nativeを利用したスマートフォンアプリ開発を経験し、技術領域を広げることである。

現時点では仕様を完全に固定せず、小さなMVPから開始し、開発しながらゲーム内容や機能を拡張していく。

---

# アプリのコンセプト

クイズや問題に回答することでキャラクターを成長させ、ダンジョンやステージを攻略していくRPG風の学習ゲームを想定している。

一般的な学校教科（国語・算数・理科・社会）だけを扱うことにはこだわらず、例えば以下のようなテーマをゲームとして学べるものを検討している。

* お金の知識

  * 買い物
  * おつり
  * 貯金
  * 収入と支出
  * 税金
  * 投資
  * リスク
  * 詐欺対策など
* スポーツ

  * サッカー
  * 野球
  * バスケットボール等のルール
* ITリテラシー
* 防災
* 生活知識

初期テーマはまだ確定していない。

「クイズアプリにRPGの見た目を付けるだけ」ではなく、可能であればゲームシステムそのものから知識を学べる設計も将来的に検討する。

例：

所持金10,000Gの状態で武器、宿泊費、移動費などを考えて行動することで、予算管理や貯蓄について自然に学ぶ。

---

# 想定するゲームシステム

基本的にはリアルタイムアクションではなく、画面遷移・選択・クイズ回答を中心としたRPGとする。

3Dゲームやリアルタイムでキャラクターを操作するゲームは想定していない。

イメージ：

```text
タイトル
  ↓
ホーム
  ↓
ワールド / ステージ選択
  ↓
ダンジョン
  ↓
敵との遭遇
  ↓
問題出題
  ↓
回答
  ↓
正解 → 攻撃
不正解 → 敵から攻撃
  ↓
敵撃破
  ↓
経験値 / ゴールド獲得
  ↓
レベルアップ / 装備購入
  ↓
次のステージ
```

プレイヤーには以下のような要素を持たせる想定。

```text
Player

- level
- exp
- hp
- attack
- defense
- gold
- equipment
- inventory
- clearedStages
```

装備については例えば、

```text
Weapon

- id
- name
- attack
- price
```

などのマスターデータを持つ。

---

# クイズシステム

初期実装ではシンプルな選択式問題から開始する。

例：

```text
12 × 4 = ?

[36] [42]
[48] [52]
```

正解すると敵へ攻撃するなど、回答結果をゲーム進行に反映する。

将来的には回答方法として以下も検討する。

* 4択
* 複数選択
* 数値入力
* 手書き入力
* 音声入力

特にスマートフォンの画面に指やApple Pencil等で直接文字を書く「手書き回答」に興味がある。

例：

```text
「やま」を漢字で書いてください

┌───────────┐
│           │
│     山     │
│           │
└───────────┘
```

将来的には、

* 数字認識
* ひらがな認識
* 漢字認識
* 筆算
* 書き順判定

などへの発展も考えられる。

ただし、これらは初期MVPには含めない。

---

# 初期技術構成

初期バージョンではサーバーを使用しない。

以下を基本構成とする。

```text
React Native
+
Expo
+
TypeScript
+
SQLite
```

開発環境はmacOS。

Dockerは使用しない。

iOS Simulatorを利用して開発を開始し、必要に応じてAndroid Emulatorおよび実機確認を追加する。

---

# データ管理

初期バージョンではすべて端末内で完結させる。

```text
React Native Application
        ↓
      SQLite
        ↓
      game.db
```

Laravel、MySQL、外部API等のバックエンドは初期段階では使用しない。

React Nativeアプリ自身がSQLiteを直接操作する。

ゲームロジックについてもTypeScriptでアプリ内に実装する。

---

# マスターデータ

ゲーム自体に含まれる固定データについては、TypeScript / JSONまたはSQLiteの初期データとして管理する。

例：

* items
* weapons
* armors
* enemies
* stages
* questions

プレイヤーによって変化するデータはSQLiteへ保存する。

例：

* プレイヤーレベル
* 経験値
* HP
* ゴールド
* 所持アイテム
* 装備
* ステージ進行状況
* 問題回答履歴

---

# SQLiteの想定用途

将来的には以下のようなデータ構造を検討する。

```text
player

- id
- level
- exp
- hp
- gold


items

- id
- name
- type
- price


player_items

- item_id
- quantity


equipment

- slot
- item_id


stages

- id
- name
- difficulty


stage_progress

- stage_id
- cleared
- score


questions

- id
- category
- difficulty
- question
- answer


question_results

- question_id
- is_correct
- answered_at
```

現段階では過剰設計を避け、MVPに必要になったものから実装する。

---

# ディレクトリ構成

最初から巨大なアーキテクチャにはせず、責務をある程度分離できる構成にしたい。

例：

```text
src/
├── screens/
│   ├── HomeScreen.tsx
│   ├── StageScreen.tsx
│   ├── BattleScreen.tsx
│   └── ShopScreen.tsx
│
├── components/
│   ├── HpBar.tsx
│   └── AnswerButton.tsx
│
├── services/
│   ├── BattleService.ts
│   ├── LevelService.ts
│   └── ShopService.ts
│
├── repositories/
│   ├── PlayerRepository.ts
│   ├── ItemRepository.ts
│   └── QuestionRepository.ts
│
├── database/
│   ├── database.ts
│   ├── migrations/
│   └── seed/
│
├── data/
│   ├── items.ts
│   ├── enemies.ts
│   └── questions.ts
│
├── types/
│
└── utils/

assets/
├── images/
├── sounds/
└── fonts/
```

これはあくまで候補であり、Expo / React Nativeの現在のベストプラクティスやプロジェクト規模を考慮して適切な構成を提案してほしい。

不要な抽象化や過剰なレイヤー分割は避ける。

---

# 将来的なバックエンド

初期バージョンでは実装しない。

将来的に以下が必要になった場合、Laravel API + MySQL等のクラウドバックエンドを追加する可能性がある。

```text
React Native
    │
    ├── SQLite
    │    └ ローカルセーブ
    │
    └── HTTPS
          ↓
       Laravel API
          ↓
        MySQL
```

想定用途：

* ユーザーアカウント
* クラウドセーブ
* 機種変更時のデータ引き継ぎ
* 複数端末同期
* ランキング
* 問題データのオンライン配信
* イベント
* お知らせ

SQLiteはLaravel導入後もローカルデータとして利用し、オンライン時にクラウドと同期する構成も検討する。

---

# 初期MVP

最初から完成版を作ろうとしない。

まず以下が動作する最小構成を目標とする。

```text
アプリ起動
   ↓
ホーム画面
   ↓
ステージ選択
   ↓
戦闘画面
   ↓
問題を1問表示
   ↓
4択から回答
   ↓
正解なら敵へダメージ
   ↓
敵を倒す
   ↓
経験値・ゴールド獲得
   ↓
SQLiteへ保存
   ↓
アプリ終了
   ↓
再起動
   ↓
以前の状態から再開
```

まずこの一連の流れを完成させる。

UIデザイン、キャラクター画像、複雑なアニメーション、手書き認識、クラウド同期等はその後に追加する。

---

# 開発方針

このプロジェクトでは以下を重視する。

1. まず動くものを小さく完成させる。
2. 初期段階ではサーバーを作らない。
3. 不要なライブラリを大量に導入しない。
4. TypeScriptを使用する。
5. React Native / Expoの現在の標準的な方法を優先する。
6. 将来的な拡張性は考慮するが、過剰設計はしない。
7. UIとゲームロジック、データアクセス処理は可能な範囲で分離する。
8. Webアプリ的な設計を無理にReact Nativeへ持ち込まず、React Nativeとして自然な設計を採用する。
9. iOSを最初の動作確認環境とするが、将来的なAndroid対応を阻害しない。
10. 学習目的の個人開発であるため、新しいスマートフォン固有技術も段階的に取り入れる。

---

# 現在行いたいこと

Gitリポジトリはすでに作成済み。

現在はまだアプリ本体の実装前であり、最初にReact Native開発環境を構築したい。

まず以下について、現在の安定したReact Native / Expo環境を前提として順番に進めたい。

1. Macの既存環境（Node.js、npm等）の確認
2. Expo + React Native + TypeScriptプロジェクトの初期化
3. 既存Gitリポジトリ内へ適切に配置
4. 必要最低限の依存パッケージ導入
5. Xcode / iOS Simulatorでアプリを起動
6. 初期画面の表示確認
7. SQLiteの導入
8. SQLiteへの簡単なデータ保存
9. アプリ再起動後もデータが保持されることを確認

一度にすべて実装せず、各ステップでコマンド・変更内容・目的を説明しながら進めること。

既存環境を破壊する可能性がある操作や、不要なグローバルインストールは避けること。

不明な環境情報が必要な場合は、推測せず確認コマンドを提示すること。
